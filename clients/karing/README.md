# Karing 配置

Karing 是基于 sing-box 的开源图形客户端，可在 iPhone 和 iPad 上通过系统 VPN
能力覆盖 App 与蜂窝网络。

1. 复制 `karing.template.yaml`，只编辑个人副本。
2. 在副本中填写分配的用户名和密码。
3. 通过“文件”或 AirDrop 导入 Karing。
4. 删除传输用副本，不要上传或提交包含密码的文件。
5. 选择 `MinBot-Selective` 并允许 iOS 添加 VPN 配置。

模板每 5 分钟读取最新 allowlist；同一账号可用于该用户自己的多台设备。
